# 🏗️ Arquitectura del Sistema Smart Park

## Diagrama de Integración Completa

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         SMART PARK - ARQUITECTURA                           │
└─────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                          CAPA DE DETECCIÓN (IA)                             │
│                                                                             │
│  ┌──────────────┐       ┌──────────────┐       ┌──────────────┐           │
│  │   Cámara     │       │  YOLO v8     │       │  OpenCV      │           │
│  │   Web/USB    │──────▶│  Detection   │──────▶│  Processing  │           │
│  └──────────────┘       └──────────────┘       └──────────────┘           │
│                                                         │                   │
│                                                         ▼                   │
│                                               ┌──────────────────┐         │
│                                               │ spot_mapping.json │         │
│                                               │  1 → A-01        │         │
│                                               │  2 → A-02        │         │
│                                               └──────────────────┘         │
│                                                         │                   │
│                       Python (parking_monitor.py)      │                   │
│                       psycopg2                          │                   │
└─────────────────────────────────────────────────────────┼───────────────────┘
                                                          │
                                                          │ INSERT/UPDATE
                                                          │ (Directo a BD)
                                                          ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CAPA DE PERSISTENCIA                                │
│                                                                             │
│                        ┌─────────────────────┐                             │
│                        │   PostgreSQL        │                             │
│                        │   Puerto: 5433      │                             │
│                        │                     │                             │
│                        │  ┌───────────────┐ │                             │
│                        │  │parking_spaces │ │  ← Espacios de parking      │
│                        │  │               │ │                             │
│                        │  │id (UUID)      │ │                             │
│                        │  │spaceCode      │ │  A-01, A-02, A-03...        │
│                        │  │status         │ │  free/occupied/unknown      │
│                        │  │updatedAt      │ │  Timestamp último cambio    │
│                        │  └───────────────┘ │                             │
│                        │                     │                             │
│                        │  ┌───────────────┐ │                             │
│                        │  │occupancy_     │ │  ← Historial de eventos     │
│                        │  │events         │ │                             │
│                        │  │               │ │                             │
│                        │  │id (UUID)      │ │                             │
│                        │  │parkingSpaceId │ │  FK a parking_spaces        │
│                        │  │status         │ │  Estado en ese momento      │
│                        │  │timestamp      │ │  Cuándo ocurrió             │
│                        │  └───────────────┘ │                             │
│                        │                     │                             │
│                        │  ┌───────────────┐ │                             │
│                        │  │users          │ │  ← Usuarios del sistema     │
│                        │  │sensors        │ │  ← Sensores (legacy)        │
│                        │  │reports        │ │  ← Reportes                 │
│                        │  └───────────────┘ │                             │
│                        └─────────────────────┘                             │
│                                  ▲                                          │
└──────────────────────────────────┼──────────────────────────────────────────┘
                                   │
                                   │ TypeORM
                                   │ (ORM)
                                   │
┌──────────────────────────────────┼──────────────────────────────────────────┐
│                         CAPA DE LÓGICA (Backend)                            │
│                                  │                                          │
│                        ┌─────────▼──────────┐                              │
│                        │   NestJS           │                              │
│                        │   Puerto: 3000     │                              │
│                        │                    │                              │
│   ┌──────────────────┐ │  API REST:         │ ┌──────────────────┐        │
│   │ Auth Module      │ │                    │ │ Parking Module   │        │
│   │ - JWT            │◀┤  /auth/login       │▶│ - CRUD espacios  │        │
│   │ - Roles          │ │  /parking/spaces   │ │ - Actualización  │        │
│   └──────────────────┘ │  /occupancy        │ └──────────────────┘        │
│                        │  /reports          │                              │
│   ┌──────────────────┐ │  /users            │ ┌──────────────────┐        │
│   │ Users Module     │ │                    │ │ Occupancy Module │        │
│   │ - Gestión users  │ │  Todos protegidos  │ │ - Eventos        │        │
│   └──────────────────┘ │  con Guards        │ └──────────────────┘        │
│                        └────────────────────┘                              │
│                                  │                                          │
└──────────────────────────────────┼──────────────────────────────────────────┘
                                   │
                                   │ HTTP/REST
                                   │ JSON
                                   │
┌──────────────────────────────────┼──────────────────────────────────────────┐
│                      CAPA DE PRESENTACIÓN (Frontend)                        │
│                                  │                                          │
│                        ┌─────────▼──────────┐                              │
│                        │   Vue.js 3         │                              │
│                        │   Puerto: 5173     │                              │
│                        │   + Vite + Tailwind│                              │
│                        │                    │                              │
│   ┌──────────────────┐ │  Componentes:      │ ┌──────────────────┐        │
│   │ Login            │ │                    │ │ Dashboard        │        │
│   │ - Autenticación  │◀┤  - Dashboard.vue   │▶│ - Vista general  │        │
│   └──────────────────┘ │  - Parking.vue     │ └──────────────────┘        │
│                        │  - Config.vue      │                              │
│   ┌──────────────────┐ │  - Reports.vue     │ ┌──────────────────┐        │
│   │ Configuración    │ │                    │ │ Parking Spaces   │        │
│   │ - Gestión users  │ │  Estado (Pinia):   │ │ - Vista plazas   │        │
│   │ - Crear espacios │ │  - authStore       │ │ - Tiempo real    │        │
│   └──────────────────┘ │  - parkingStore    │ └──────────────────┘        │
│                        │                    │                              │
│                        │  Polling: 5 seg    │                              │
│                        └────────────────────┘                              │
│                                  │                                          │
│                                  ▼                                          │
│                        ┌────────────────────┐                              │
│                        │   Usuario Final    │                              │
│                        │   Navegador Web    │                              │
│                        └────────────────────┘                              │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                               FLUJO DE DATOS
═══════════════════════════════════════════════════════════════════════════════

ESCENARIO 1: Detección de Vehículo (IA → BD → Frontend)
─────────────────────────────────────────────────────────

1. Cámara captura frame
   │
   ▼
2. YOLO detecta vehículo en coordenadas (x1, y1, x2, y2)
   │
   ▼
3. Sistema verifica intersección con plaza ID 2
   │
   ▼
4. Consulta spot_mapping.json: 2 → "A-02"
   │
   ▼
5. Python ejecuta:
   SELECT id FROM parking_spaces WHERE spaceCode = 'A-02'
   → Obtiene UUID: "a1b2c3d4-5678-..."
   │
   ▼
6. Verifica estado actual:
   SELECT status FROM parking_spaces WHERE id = 'UUID'
   → Estado actual: "free"
   │
   ▼
7. Actualiza base de datos:
   UPDATE parking_spaces 
   SET status = 'occupied', updatedAt = NOW() 
   WHERE id = 'UUID'
   │
   ▼
8. Crea evento histórico:
   INSERT INTO occupancy_events 
   (parkingSpaceId, status, timestamp) 
   VALUES ('UUID', 'occupied', NOW())
   │
   ▼
9. Backend ya tiene datos actualizados (usa misma BD)
   │
   ▼
10. Frontend hace polling (cada 5 seg):
    GET http://localhost:3000/parking/spaces
    │
    ▼
11. Backend responde con estado actual desde PostgreSQL
    │
    ▼
12. Frontend actualiza UI:
    Plaza A-02: OCUPADA ✅


ESCENARIO 2: Consulta de Usuario (Frontend → Backend → BD)
───────────────────────────────────────────────────────────

1. Usuario abre navegador: http://localhost:5173
   │
   ▼
2. Frontend solicita estado:
   GET http://localhost:3000/parking/spaces
   │
   ▼
3. Backend (NestJS) consulta:
   SELECT * FROM parking_spaces ORDER BY spaceCode
   │
   ▼
4. PostgreSQL devuelve:
   [
     {id: "...", spaceCode: "A-01", status: "free", updatedAt: "..."},
     {id: "...", spaceCode: "A-02", status: "occupied", updatedAt: "..."},
     ...
   ]
   │
   ▼
5. Backend serializa y envía JSON
   │
   ▼
6. Frontend recibe y renderiza:
   🟩 A-01: LIBRE
   ✅ A-02: OCUPADA
   🟩 A-03: LIBRE
   ✅ A-04: OCUPADA


═══════════════════════════════════════════════════════════════════════════════
                         TECNOLOGÍAS POR CAPA
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────┬──────────────────────────────────────────────────────────┐
│ CAPA            │ TECNOLOGÍAS                                              │
├─────────────────┼──────────────────────────────────────────────────────────┤
│ Detección (IA)  │ - Python 3.8+                                            │
│                 │ - YOLO v8 (Ultralytics)                                  │
│                 │ - OpenCV                                                 │
│                 │ - psycopg2 (PostgreSQL driver)                           │
│                 │ - NumPy                                                  │
├─────────────────┼──────────────────────────────────────────────────────────┤
│ Persistencia    │ - PostgreSQL 15                                          │
│                 │ - Docker (containerización)                              │
├─────────────────┼──────────────────────────────────────────────────────────┤
│ Backend         │ - NestJS (Node.js framework)                             │
│                 │ - TypeORM (ORM)                                          │
│                 │ - TypeScript                                             │
│                 │ - JWT (autenticación)                                    │
│                 │ - Passport.js                                            │
│                 │ - bcrypt (hashing)                                       │
├─────────────────┼──────────────────────────────────────────────────────────┤
│ Frontend        │ - Vue.js 3 (Composition API)                             │
│                 │ - Vite (build tool)                                      │
│                 │ - TypeScript                                             │
│                 │ - Tailwind CSS                                           │
│                 │ - Pinia (state management)                               │
│                 │ - Vue Router                                             │
└─────────────────┴──────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                            PUERTOS Y CONEXIONES
═══════════════════════════════════════════════════════════════════════════════

PostgreSQL:   5432 (interno Docker) → 5433 (expuesto al host)
              ├─ Conexión desde IA: localhost:5433
              └─ Conexión desde Backend: localhost:5433 (o postgres:5432 si ambos en Docker)

Backend:      3000
              └─ API REST disponible en http://localhost:3000

Frontend:     5173 (Vite dev server)
              └─ Interfaz web en http://localhost:5173


═══════════════════════════════════════════════════════════════════════════════
                         BENEFICIOS DE LA INTEGRACIÓN
═══════════════════════════════════════════════════════════════════════════════

✅ ANTES (Sistemas separados):
   - IA guardaba en MySQL
   - Backend usaba PostgreSQL
   - Frontend no veía detecciones de IA
   - Datos duplicados e inconsistentes

✅ AHORA (Sistema integrado):
   - Una sola base de datos (PostgreSQL)
   - IA actualiza directamente la BD del backend
   - Frontend recibe todo en tiempo real
   - Historial completo unificado
   - Mantenimiento simplificado
   - Escalabilidad mejorada


═══════════════════════════════════════════════════════════════════════════════
                              SEGURIDAD
═══════════════════════════════════════════════════════════════════════════════

Backend:
  - ✅ Autenticación JWT
  - ✅ Guards por rol (Admin/User)
  - ✅ Passwords hasheados con bcrypt
  - ✅ Validación de datos con class-validator

Frontend:
  - ✅ Token almacenado en localStorage
  - ✅ Guards de rutas (Vue Router)
  - ✅ Logout seguro

Base de Datos:
  - ✅ Usuario con permisos limitados
  - ✅ Conexiones por password
  - ✅ Puerto no expuesto públicamente (solo localhost)


═══════════════════════════════════════════════════════════════════════════════
                           ESCALABILIDAD FUTURA
═══════════════════════════════════════════════════════════════════════════════

Posibles mejoras:

1. WebSockets para actualizaciones instantáneas (sin polling)
2. Múltiples cámaras simultáneas
3. Balanceo de carga para el backend
4. Redis para caché
5. App móvil nativa (React Native / Flutter)
6. Machine Learning para predicción de ocupación
7. Integración con sistemas de pago
8. API Gateway para microservicios


═══════════════════════════════════════════════════════════════════════════════

Versión: 2.0.0
Fecha: Noviembre 2025
Estado: ✅ Producción
```
