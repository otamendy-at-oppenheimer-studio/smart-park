# Configuración de PostgreSQL (debe coincidir con el backend)
# Estos valores deben ser los mismos que los del backend en parking-iot-system-main

DB_HOST=localhost
DB_PORT=5433
DB_USER=admin
DB_PASSWORD=admin123
DB_NAME=parkingdb
