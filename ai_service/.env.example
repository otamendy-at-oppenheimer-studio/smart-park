# Configuración de Oracle Database (debe coincidir con el backend)
# Estos valores deben ser los mismos que los del backend

DB_HOST=localhost
DB_PORT=1521
DB_USER=parkingapp
DB_PASSWORD=admin123
DB_SID=FREEPDB1
